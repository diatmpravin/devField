<%= form_for(@state) do |f| %>
  <% if @state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% @state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :raw_state %><br />
    <%= f.text_field :raw_state %>
  </div>
  <div class="field">
    <%= f.label :clean_state %><br />
    <%= f.text_field :clean_state %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
