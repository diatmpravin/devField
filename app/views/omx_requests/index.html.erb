<h1>Listing omx_requests</h1>

<table>
  <tr>
    <th>Ordermotion request</th>
    <th>Request type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @omx_requests.each do |omx_request| %>
  <tr>
    <td><%= omx_request.ordermotion_request_id %></td>
    <td><%= omx_request.request_type %></td>
    <td><%= link_to 'Show', omx_request %></td>
    <td><%= link_to 'Edit', edit_omx_request_path(omx_request) %></td>
    <td><%= link_to 'Destroy', omx_request, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Omx request', new_omx_request_path %>
